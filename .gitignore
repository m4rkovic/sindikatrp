# Dependencies / build
node_modules/
.next/
out/

# Runtime data — baza, ključ i upload fajlovi ne idu u repo.
# Baza sadrži admin hash i sesije; secret.key potpisuje sesije.
data/*.db
data/*.db-shm
data/*.db-wal
data/secret.key
data/uploads/

# Env
.env
.env.local
.env.production

# OS / editor
.DS_Store
Thumbs.db
